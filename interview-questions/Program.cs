using System;
using interview_questions.geeks4geeks.Arrays;
using interview_questions.geeks4geeks.DP;
using interview_questions.geeks4geeks.Strings;
using interview_questions.geeks4geeks.Trees;
using interview_questions.InterviewCake;

namespace interview_questions
{
    class Program
    {
        // Given an array arr[] of positive integers of size N. Reverse every sub-array of K group elements.
        static void Main(string[] args)
        {
            StringSegmentation.Test();
            //DeleteAGivenNodeFromASortedSinglyLinkedList.Test();
            //ReverseLinkedList.Test();
            //SearchInSortedAndRotatedArray.Test();
            //FindEitherLinkedListHasCycles.Test();
            Console.WriteLine("press any key");
            Console.ReadKey();

            // facebook qs:
            // Design "stories"(like Instagram and Whatsapp).Stories disappear after friends view them, and are only available for 24 hours after publishing.
            // LRU Cache
            // Clone Graph
            // K closest points to origin
            // count indices in a 2d array(LC easy),
            // write an algo to calc runtime given a stack trace as input (LC medium)  
        }
    }
}
